#!/bin/bash 

if [ $# = 0 ] ; then
	echo "USAGE: $0 USERNAME <PORT>" 
	exit 1
fi

USERNAME=$1 # The user/level for the host
HOST=bandit.labs.overthewire.org # The host for the wargame.

if [ $2 ] ; then
	PORT=$2
else 
	PORT=2220
fi

ssh $USERNAME@$HOST -p $PORT
